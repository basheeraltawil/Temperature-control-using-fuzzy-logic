[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

# Package metadata lives in setup.cfg so that it also builds with the older
# setuptools shipped by Ubuntu 22.04 / ROS 2 Humble.

[tool.pytest.ini_options]
testpaths = ["tests"]
